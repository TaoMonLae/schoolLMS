import { PageHeader } from "@/components/page-header";
import { createSubject } from "@/app/dashboard/lms/actions";
import { canManageLms } from "@/lib/rbac";
import { getRequiredCurrentUser } from "@/lib/session";

export default async function NewSubjectPage() { const user = await getRequiredCurrentUser(); if (!canManageLms(user.role)) return <PageHeader eyebrow="LMS" title="Not authorized" description="You cannot create subjects." />; return <div className="space-y-6 pb-10"><PageHeader eyebrow="LMS" title="Create Subject" description="Create a GED or custom subject for this school." /><form action={createSubject} className="grid gap-4 rounded-lg border border-hairline bg-canvas p-5 shadow-soft sm:grid-cols-2"><Input name="name" label="Name" required /><Input name="code" label="Code" /><label className="flex items-center gap-2 text-sm font-semibold text-ink"><input type="checkbox" name="isGed" /> GED subject</label><label className="sm:col-span-2 text-sm font-semibold text-ink">Description<textarea name="description" rows={5} className="mt-2 w-full rounded-md border border-hairline px-3 py-3 text-sm" /></label><div className="sm:col-span-2"><button className="rounded-md bg-ink px-5 py-3 text-sm font-bold text-on-dark">Create Subject</button></div></form></div>; }
function Input({ name, label, required }: { name: string; label: string; required?: boolean }) { return <label className="text-sm font-semibold text-ink">{label}<input name={name} required={required} className="mt-2 w-full rounded-md border border-hairline px-3 py-3 text-sm" /></label>; }
